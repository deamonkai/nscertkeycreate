#!/usr/bin/env python3
"""nsctl — thin wrapper for `nscert` CLI.

This script simply forwards arguments to `nscert.cli.main` so it can be
used directly from $PATH when installed or during development.
"""
import sys

from nscert.cli import main


if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
